{
  "name": "cypress-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run --config baseUrl=https://the-internet.herokuapp.com",
    "cy:open": "cypress open --config baseUrl=https://the-internet.herokuapp.com",
    "format": "prettier --write \"cypress/**/*.{js,jsx}\" && eslint \"cypress/**/*.{js,jsx}\"",
    "prettier:check" : "prettier --check",
    "prettier:write" :"prettier --write .", 
    "jenkins": "java -jar jenkins.war -httPort=8080 --enable-future-java"
  },
  "author": "adelinop",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.2.0",
    "cypress-mochawesome-reporter": "^3.2.3",
    "eslint": "^8.30.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-cypress": "^2.12.1",
    "prettier": "^2.8.1"
  }
}
